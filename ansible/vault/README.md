# Ansible Vault Secrets Directory

This directory is used to store encrypted secrets (like `.env.vault`).
Files in this directory should be encrypted using `ansible-vault` before being committed to Git.
